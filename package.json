{
  "name": "stale-issue-cleanup",
  "version": "7.1.1",
  "description": "GitHub Action that cleans up old and response-reqested issues",
  "private": true,
  "main": "dist/entrypoint.js",
  "scripts": {
    "check-lint": "prettier --list-different src/**.js && eslint src/**.js",
    "lint": "biome check --write src",
    "test": "npm run lint && vitest run && npm run build",
    "build": "tsc",
    "package": "npm run build && ncc build -m --license THIRD-PARTY -o dist",
    "coverage": "vitest run --coverage"
  },
  "engines": {
    "node": ">=24"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/step-security/stale-issue-cleanup"
  },
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/step-security/stale-issue-cleanup/issues"
  },
  "homepage": "https://github.com/step-security/stale-issue-cleanup/#readme",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1",
    "axios": "^1.15.2",
    "dateformat": "^4.4.1"
  },
  "devDependencies": {
    "@biomejs/biome": "2.1.2",
    "@fetch-mock/vitest": "^0.2.14",
    "@octokit/plugin-rest-endpoint-methods": "^13.3.0",
    "@types/dateformat": "^5.0.3",
    "@types/node": "^24.1.0",
    "@vercel/ncc": "^0.38.3",
    "@vitest/coverage-v8": "^3.0.0",
    "dotenv": "^17.4.2",
    "typescript": "^5.8.3",
    "vitest": "^3.0.0"
  }
}
